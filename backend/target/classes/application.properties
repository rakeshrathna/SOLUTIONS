# ============================================================
# EDUIDEAL POSTGRESQL BACKEND CONFIGURATION
# ============================================================

server.port=8080

# PostgreSQL Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/eduideal
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:rakeshgenius}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Jackson Config
spring.jackson.serialization.fail-on-empty-beans=false

# App JWT Secret
app.jwt.secret=LearnovaEduidealPostgresqlSecretKey2026SecureTokenKey998877665544332211
app.jwt.expiration-ms=86400000
