spring.application.name=eduideal-portal

# Supabase Postgres Database Configuration (Externalized credentials with default fallbacks)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-0-ap-southeast-2.pooler.supabase.com:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres.rfgbnjocvrhtaivxrlsx}
spring.datasource.password=${DB_PASSWORD:Kishore@31012008}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA & Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
app.jwt.secret=${APP_JWT_SECRET:EduIdealAcademySuperSecretJwtKey2026Secure256BitMinimumLengthSecret}
app.jwt.expiration-ms=86400000

# CORS Configuration
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}

# Server Configuration
server.port=${PORT:8080}